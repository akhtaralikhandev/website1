import ReactDOM, { createRoot } from "react-dom/client";
import Webpage from "./components/css_html";
import Modal from "./components/modal";
import Home from "./website1";
const root = createRoot(document.getElementById("root"));

root.render(<Home />);
